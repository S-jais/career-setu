{
  "name": "careersetu",
  "version": "1.0.0",
  "private": true,
  "description": "CareerSetu — India-First AI Career & Academia-Industry Collaboration Platform",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix \"[{name}]\" --names \"BACKEND,FRONTEND,AI\" --prefix-colors \"blue.bold,green.bold,magenta.bold\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\"",
    "dev:backend": "node scripts/dev-backend.js",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:ai": "node scripts/dev-ai.js",
    "dev:all": "npm run dev",
    "start": "npm run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
